<nav>

    <div class="links nav-collapse">
        <ul>
            {{#foreach navigation}}
                <a href="{{url absolute="true"}}">
                <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation">{{label}}</li>
                </a>
            {{/foreach}}
        </ul>
    </div>

</nav>
